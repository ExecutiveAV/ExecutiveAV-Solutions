import React from 'react';

const NewEntryPortal = ({ newEntryType }) => {
    switch (newEntryType) {
        case "company":
            return (
                <section >
                    test
                </section>
            )
            
        default:
            
    }
};

export default NewEntryPortal;